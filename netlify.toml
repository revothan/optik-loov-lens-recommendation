[build]
  command = "npm ci && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
  NPM_FLAGS = "--strict-peer-deps=false"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
